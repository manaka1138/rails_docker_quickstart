<%= form_with(model: @upload_file, local: true) do |form| %>
  <% if @upload_file.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@upload_file.errors.count, "error") %> prohibited
      this upload from being saved:</h2>
    <ul>
    <% @upload_file.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
  <p>
    <%= form.label :title %><br>
    <%= form.text_field :title %>
  </p>

  <p>
    <%= form.label :text %><br>
    <%= form.text_area :text %>
  </p>

  <p>
    <%= form.submit %>
  </p>
<% end %>

<%= link_to 'Back', uploader_index_path %>
